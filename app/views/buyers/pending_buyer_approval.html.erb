<div class="container-fluid mt-10 card shadow-sm bg-white">
  <div class="card-header">
    <div class="row align-items-center">
      <div class="col">
        <h2 class="h5"><%= @buyer.name %> - Clarification Approval</h2>
      </div>
      <div class="col text-right">
        <%= link_to 'Back', pending_approval_path, class: 'btn btn-black btn-sm' %>
      </div>
    </div>
  </div>
  <% if @presenter.return_pba.any? %>
    <div id="couriers_table" class="mt-10">
      <%= render 'pending_buyer_approval' %>
    </div>
  <% else %>
    <div class="alert alert-info mt-4" role="alert">
      <strong>No more pending items for this Vendor and Courier.</strong>
    </div>
  <% end %>
</div>
